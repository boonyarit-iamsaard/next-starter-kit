#
# CI Environment Variables
#
# This file contains mock environment variables used by GitHub Actions CI workflow.
# Purpose: Provide valid environment values for type checking and build validation
# without requiring real secrets or database connections.
#
# Usage:
#   - Copied to .env during CI runs
#   - Used with SKIP_ENV_VALIDATION=true for fast static analysis
#   - All values are safe mock data - no real credentials
#
# Maintenance:
#   - Keep in sync with .env.example structure
#   - Update when new environment variables are added
#   - Use consistent mock patterns for security and clarity
#

#
# Application environment variables
#
APP_NAME="CI Mock App"
BETTER_AUTH_SECRET="ci-mock-secret-32-characters-minimum-length-required"
BETTER_AUTH_URL="http://localhost:3000"
DATABASE_URL="postgresql://ci-mock:ci-mock@localhost:5432/ci-mock"
GOOGLE_CLIENT_ID="ci-mock-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="ci-mock-google-client-secret"
MAIL_FROM_ADDRESS="ci-mock@example.com"
MAIL_FROM_NAME="CI Mock Mailer"
MAIL_HOST="localhost"
MAIL_PASSWORD="ci-mock-password"
MAIL_PORT="587"
MAIL_USER="ci-mock-user"
NODE_ENV="test"

#
# Client-side Environment Variables
#
NEXT_PUBLIC_APP_NAME="CI Mock App"
NEXT_PUBLIC_APP_URL="http://localhost:3000"